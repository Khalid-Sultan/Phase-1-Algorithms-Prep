using System;
using System.Collections.Generic;
using System.Text;

namespace Day_19
{
    //class First_Bad_Version : VersionControl
    //{
    //    /* The isBadVersion API is defined in the parent class VersionControl.
    //          bool IsBadVersion(int version); */
    //    public int FirstBadVersion(int n)
    //    {
    //        int lowest = 1;
    //        int max = n;
    //        while (lowest < max)
    //        {
    //            if (IsBadVersion(lowest + (max - lowest) / 2) == true)
    //            {
    //                max = lowest + (max - lowest) / 2;
    //            }
    //            else
    //            {
    //                lowest = 1 + lowest + (max - lowest) / 2;
    //            }
    //        }
    //        return lowest;
    //    }
    //}
}

